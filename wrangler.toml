name = "hook-cashier"
compatibility_date = "2024-09-19"

# Workers static assets (SPA) — required because deploy command is `npx wrangler deploy`
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
